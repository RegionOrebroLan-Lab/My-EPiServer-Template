@model PageModel
<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
	@if(Model.View.Layout.MainNavigation.Content != null)
	{
		<a class="navbar-brand" href="@Model.View.Layout.MainNavigation.Url">@Model.View.Layout.MainNavigation.Text</a>
	}
	@if(Model.View.Layout.MainNavigation.Any() || Model.View.Layout.CultureNavigation.Count > 1)
	{
		<button aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler" data-toggle="collapse" data-target="#navbar" type="button">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbar">
			<ul class="navbar-nav mr-auto">
				@foreach(var node in Model.View.Layout.MainNavigation.MaximumItems(10))
				{
					<li class="nav-item">
						<a class="nav-link@(node.Active || node.ActiveAncestor ? " active" : string.Empty)" href="@node.Url">@node.Text</a>
					</li>
				}
			</ul>
			<partial name="~/Views/Shared/Parts/Navigation/CultureNavigation.cshtml" />
		</div>
	}
</nav>